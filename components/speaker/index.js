import React, { Fragment } from "react";

// import components
import ChannelHeader from "../channelHeader";

const Speaker = () => {
  return (
    <Fragment>
      <ChannelHeader />
    </Fragment>
  );
};

export default Speaker;
